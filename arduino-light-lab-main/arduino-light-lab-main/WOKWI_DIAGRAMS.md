# 🎨 WOKWI ARCHITECTURE - VISUAL DIAGRAMS

## 📊 Complete System Architecture

```
┌─────────────────────────────────────────────────────────────────────┐
│                          USER INTERFACE                             │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐              │
│  │ LCDComponent │  │ServoComponent│  │ LEDComponent │              │
│  │   (React)    │  │   (React)    │  │   (React)    │              │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘              │
│         │                 │                 │                       │
│         │ Listen          │ Listen          │ Listen                │
│         ↓                 ↓                 ↓                       │
└─────────────────────────────────────────────────────────────────────┘
          │                 │                 │
          │ Events          │ Events          │ Events
          ↓                 ↓                 ↓
┌─────────────────────────────────────────────────────────────────────┐
│                       OBSERVER ENGINES                              │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐              │
│  │  LCDEngine   │  │ ServoEngine  │  │  LEDEngine   │              │
│  │              │  │              │  │              │              │
│  │ • Decode     │  │ • Calculate  │  │ • Track      │              │
│  │   commands   │  │   angle      │  │   state      │              │
│  │ • Maintain   │  │ • Emit       │  │ • Emit       │              │
│  │   buffer     │  │   events     │  │   events     │              │
│  │ • Emit       │  │              │  │              │              │
│  │   events     │  │              │  │              │              │
│  └──────▲───────┘  └──────▲───────┘  └──────▲───────┘              │
│         │                 │                 │                       │
│         │ Notify          │ Notify          │ Notify                │
└─────────┼─────────────────┼─────────────────┼─────────────────────┘
          │                 │                 │
          │ PORT changes    │ Timer1 changes  │ PORT changes
          │                 │                 │
┌─────────┼─────────────────┼─────────────────┼─────────────────────┐
│         │                 │                 │                       │
│  ┌──────┴───────────────────────────────────┴───────┐              │
│  │         HARDWARE ABSTRACTION LAYER (HAL)         │              │
│  │                                                   │              │
│  │  • Route port changes to engines                 │              │
│  │  • Route timer changes to engines                │              │
│  │  • Translate AVR pins to component instances     │              │
│  └──────▲────────────────────────────────────────────┘              │
│         │                                                           │
│         │ Read registers (OBSERVE ONLY)                            │
│         │                                                           │
│  ┌──────┴────────────────────────────────────────────┐              │
│  │            AVR8JS WRAPPER                         │              │
│  │                                                   │              │
│  │  • checkPortChanges()  → Read PORTB/C/D          │              │
│  │  • observeTimer1()     → Read ICR1/OCR1A/OCR1B   │              │
│  │  • simulateADC()       → Read ADC registers      │              │
│  │                                                   │              │
│  │  ⚠️ CRITICAL: Only READ, never WRITE             │              │
│  └──────▲────────────────────────────────────────────┘              │
│         │                                                           │
│         │ Execute & Tick                                            │
│         │                                                           │
│  ┌──────┴────────────────────────────────────────────┐              │
│  │              REAL AVR CPU (avr8js)                │              │
│  │                                                   │              │
│  │  • avrInstruction(cpu)  ← Execute instruction    │              │
│  │  • cpu.tick()           ← Advance cycles         │              │
│  │  • Timer0               ← millis() / micros()    │              │
│  │                                                   │              │
│  │  Registers:                                       │              │
│  │    PORTB, PORTC, PORTD  ← Pin states             │              │
│  │    ICR1, OCR1A, OCR1B   ← Timer1 (Servo)         │              │
│  │    TCNT0, OCR0A, OCR0B  ← Timer0 (millis)        │              │
│  │    ADMUX, ADCL, ADCH    ← ADC (analog)           │              │
│  └──────▲────────────────────────────────────────────┘              │
│         │                                                           │
│         │ Load program                                              │
│         │                                                           │
│  ┌──────┴────────────────────────────────────────────┐              │
│  │              HEX FILE (Compiled Code)             │              │
│  │                                                   │              │
│  │  • Arduino sketch compiled by arduino-cli        │              │
│  │  • Contains AVR machine code                     │              │
│  │  • Loaded into CPU program memory                │              │
│  └───────────────────────────────────────────────────┘              │
│                                                                     │
│                      EMULATOR CORE                                  │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 🔄 Data Flow: LCD Example

```
Step 1: Arduino Code Executes
┌─────────────────────────────────┐
│ digitalWrite(EN, HIGH);         │  ← Arduino code
│ delayMicroseconds(1);           │
│ digitalWrite(EN, LOW);          │
│ delayMicroseconds(37);          │
└────────────┬────────────────────┘
             ↓
┌─────────────────────────────────┐
│ avrInstruction(cpu)             │  ← AVR8js executes
│ cpu.tick() × N cycles           │     real instructions
└────────────┬────────────────────┘
             ↓
┌─────────────────────────────────┐
│ PORTB register changes          │  ← Register update
│   Bit 3 (EN): 1 → 0             │     (falling edge)
└────────────┬────────────────────┘
             ↓
┌─────────────────────────────────┐
│ checkPortChanges() detects      │  ← Observer detects
│   currentPortB ≠ prevPortB      │     change
└────────────┬────────────────────┘
             ↓
┌─────────────────────────────────┐
│ hal.writePort(0x05, portB)      │  ← HAL routes to
│   → LCDEngine.onPinChange()     │     LCD engine
└────────────┬────────────────────┘
             ↓
┌─────────────────────────────────┐
│ LCDEngine detects EN edge       │  ← Engine processes
│   lastEnState=1, level=0        │     falling edge
│   → latchData()                 │
└────────────┬────────────────────┘
             ↓
┌─────────────────────────────────┐
│ Read D4-D7 pins                 │  ← Engine reads
│ Assemble nibbles → byte         │     pin states
│ Decode command/data             │
│ Update display buffer           │
└────────────┬────────────────────┘
             ↓
┌─────────────────────────────────┐
│ LCDComponent.render()           │  ← UI displays
│   Shows "Hello World!"          │     buffer
└─────────────────────────────────┘
```

**Key Points:**
- ✅ Arduino code executes naturally
- ✅ Registers update naturally
- ✅ Observer detects changes
- ✅ Engine processes changes
- ✅ UI displays result
- ❌ **NO manipulation** of Arduino code or timing

---

## 🦾 Data Flow: Servo Example

```
Step 1: Arduino Code Executes
┌─────────────────────────────────┐
│ #include <Servo.h>              │  ← Arduino code
│ Servo myServo;                  │
│ myServo.attach(9);              │
│ myServo.write(90);              │
└────────────┬────────────────────┘
             ↓
┌─────────────────────────────────┐
│ Servo library initializes       │  ← Servo.cpp
│   ICR1 = 40000                  │     (Arduino library)
│   OCR1A = 3000                  │
│   TCCR1A = 0b10100010           │
│   TCCR1B = 0b00011010           │
└────────────┬────────────────────┘
             ↓
┌─────────────────────────────────┐
│ avrInstruction(cpu)             │  ← AVR8js executes
│ cpu.tick() × N cycles           │     Servo library code
└────────────┬────────────────────┘
             ↓
┌─────────────────────────────────┐
│ Timer1 registers update         │  ← Register update
│   ICR1 = 40000                  │
│   OCR1A = 3000                  │
└────────────┬────────────────────┘
             ↓
┌─────────────────────────────────┐
│ observeTimer1() detects         │  ← Observer detects
│   icr1 === 40000                │     Servo signature
│   ocr1a !== prevOCR1A           │
└────────────┬────────────────────┘
             ↓
┌─────────────────────────────────┐
│ servoEngine.setAngleFromTimer1()│  ← Engine calculates
│   pulseWidth = (3000/40000)*20ms│     angle
│   pulseWidth = 1500μs           │
│   angle = 90°                   │
└────────────┬────────────────────┘
             ↓
┌─────────────────────────────────┐
│ servoEngine.notifyAngleChange() │  ← Engine emits
│   → listeners.forEach()         │     event
└────────────┬────────────────────┘
             ↓
┌─────────────────────────────────┐
│ ServoComponent receives event   │  ← UI updates
│   setAngle(90)                  │
│   <g transform="rotate(90)">    │
└─────────────────────────────────┘
```

**Key Points:**
- ✅ Servo library code executes naturally
- ✅ Timer1 registers update naturally
- ✅ Observer detects register changes
- ✅ Engine calculates angle from registers
- ✅ UI rotates SVG
- ❌ **NO PWM generation** needed
- ❌ **NO pin toggling** needed

---

## ⚡ Timing Diagram: LCD EN Pulse

```
Time (μs)  │ EN Pin │ Action
───────────┼────────┼─────────────────────────────
    0      │   0    │ Initial state
    1      │   1    │ Arduino: digitalWrite(EN, HIGH)
           │        │   ↓ AVR executes instruction
           │        │   ↓ PORTB bit 3 = 1
           │        │   ↓ checkPortChanges() detects
           │        │   ↓ LCDEngine: lastEnState = 1
    2      │   1    │ Arduino: delayMicroseconds(1)
           │        │   ↓ AVR executes delay loop
           │        │   ↓ cpu.tick() × 16 cycles
    3      │   0    │ Arduino: digitalWrite(EN, LOW)
           │        │   ↓ AVR executes instruction
           │        │   ↓ PORTB bit 3 = 0
           │        │   ↓ checkPortChanges() detects
           │        │   ↓ LCDEngine: EN edge detected!
           │        │   ↓ latchData() called
           │        │   ↓ Read D4-D7, assemble byte
    4-40   │   0    │ Arduino: delayMicroseconds(37)
           │        │   ↓ AVR executes delay loop
           │        │   ↓ cpu.tick() × 592 cycles
```

**Critical Observations:**
- ✅ EN pulse width = 1μs (preserved by real execution)
- ✅ Setup time before EN = natural instruction timing
- ✅ Hold time after EN = 37μs delay
- ✅ All timing comes from **real AVR execution**
- ❌ **NO artificial timing injection**

---

## 🎯 Register Observation Pattern

```
┌─────────────────────────────────────────────────────────┐
│                   AVR8jsWrapper                         │
│                                                         │
│  Every step():                                          │
│    1. avrInstruction(cpu)      ← Execute instruction   │
│    2. cpu.tick() × N           ← Advance cycles        │
│    3. checkPortChanges()       ← Observe PORTS         │
│    4. observeTimer1()          ← Observe Timer1        │
│    5. simulateADC()            ← Observe ADC           │
│                                                         │
│  ┌────────────────────────────────────────────────┐    │
│  │ checkPortChanges()                             │    │
│  │                                                │    │
│  │  const currentPortB = cpu.data[PORTB];        │    │
│  │  const currentPortC = cpu.data[PORTC];        │    │
│  │  const currentPortD = cpu.data[PORTD];        │    │
│  │                                                │    │
│  │  if (currentPortB !== prevPortB) {            │    │
│  │      hal.writePort(0x05, currentPortB);       │    │
│  │      prevPortB = currentPortB;                │    │
│  │  }                                             │    │
│  │                                                │    │
│  │  // Same for PORTC, PORTD                     │    │
│  └────────────────────────────────────────────────┘    │
│                                                         │
│  ┌────────────────────────────────────────────────┐    │
│  │ observeTimer1()                                │    │
│  │                                                │    │
│  │  const icr1 = cpu.data[ICR1L] |               │    │
│  │               (cpu.data[ICR1H] << 8);         │    │
│  │  const ocr1a = cpu.data[OCR1AL] |             │    │
│  │                (cpu.data[OCR1AH] << 8);       │    │
│  │                                                │    │
│  │  if (icr1 === 40000 && !timer1Initialized) {  │    │
│  │      servoEngine.setAngleFromTimer1(...);     │    │
│  │      timer1Initialized = true;                │    │
│  │  }                                             │    │
│  │                                                │    │
│  │  if (ocr1a !== prevOCR1A) {                   │    │
│  │      servoEngine.setAngleFromTimer1(...);     │    │
│  │      prevOCR1A = ocr1a;                       │    │
│  │  }                                             │    │
│  └────────────────────────────────────────────────┘    │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

**Pattern:**
1. Read register value
2. Compare with previous value
3. If changed, notify engine
4. Update previous value
5. **NEVER write back to CPU**

---

## 🔍 Comparison: Wrong vs Right

### ❌ WRONG: Manipulating CPU

```
┌─────────────────────────────────┐
│ User clicks "Move Servo to 90°" │
└────────────┬────────────────────┘
             ↓
┌─────────────────────────────────┐
│ ServoComponent.onClick()        │
│   cpu.data[OCR1A] = 3000        │  ← ❌ UI writes to CPU!
└────────────┬────────────────────┘
             ↓
┌─────────────────────────────────┐
│ CPU register changed externally │  ← ❌ Breaks simulation!
│ Arduino code doesn't know       │
│ Servo library confused          │
└─────────────────────────────────┘

Result: ❌ Simulation broken, Arduino code bypassed
```

### ✅ RIGHT: Observer Pattern

```
┌─────────────────────────────────┐
│ Arduino code executes:          │
│   myServo.write(90);            │
└────────────┬────────────────────┘
             ↓
┌─────────────────────────────────┐
│ Servo library executes:         │
│   OCR1A = 3000                  │  ← ✅ Natural execution
└────────────┬────────────────────┘
             ↓
┌─────────────────────────────────┐
│ observeTimer1() detects change  │  ← ✅ Observer watches
│   servoEngine.setAngle(90)      │
└────────────┬────────────────────┘
             ↓
┌─────────────────────────────────┐
│ servoEngine emits event         │  ← ✅ Event system
│   listeners.forEach(...)        │
└────────────┬────────────────────┘
             ↓
┌─────────────────────────────────┐
│ ServoComponent updates          │  ← ✅ UI reacts
│   <g transform="rotate(90)">    │
└─────────────────────────────────┘

Result: ✅ Simulation accurate, Arduino code in control
```

---

## 📊 State Flow Diagram

```
                    ┌──────────────┐
                    │  HEX Loaded  │
                    └──────┬───────┘
                           ↓
                    ┌──────────────┐
                    │  CPU Reset   │
                    │  PC = 0x0000 │
                    └──────┬───────┘
                           ↓
                    ┌──────────────┐
                    │ Execution    │
                    │   Started    │
                    └──────┬───────┘
                           ↓
        ┌──────────────────┴──────────────────┐
        │                                     │
        ↓                                     ↓
┌───────────────┐                    ┌───────────────┐
│  Instruction  │                    │   Observers   │
│   Execution   │                    │    Watch      │
│               │                    │   Registers   │
│ • Fetch       │                    │               │
│ • Decode      │                    │ • PORTB/C/D   │
│ • Execute     │                    │ • Timer1      │
│ • Update PC   │                    │ • ADC         │
└───────┬───────┘                    └───────┬───────┘
        │                                     │
        │ Registers                           │
        │  Updated                            │
        │                                     │
        └──────────────────┬──────────────────┘
                           ↓
                    ┌──────────────┐
                    │   Engines    │
                    │   Process    │
                    │   Changes    │
                    └──────┬───────┘
                           ↓
                    ┌──────────────┐
                    │     UI       │
                    │   Updates    │
                    └──────────────┘
```

---

## 🎨 Component Interaction Diagram

```
┌─────────────────────────────────────────────────────────────────┐
│                         React UI Layer                          │
│                                                                 │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐          │
│  │     LCD      │  │    Servo     │  │     LED      │          │
│  │  Component   │  │  Component   │  │  Component   │          │
│  └──────▲───────┘  └──────▲───────┘  └──────▲───────┘          │
│         │                 │                 │                   │
│         │ useState        │ useState        │ useState          │
│         │ useEffect       │ useEffect       │ useEffect         │
└─────────┼─────────────────┼─────────────────┼───────────────────┘
          │                 │                 │
          │ onChange        │ onChange        │ onChange
          │ event           │ event           │ event
          │                 │                 │
┌─────────┼─────────────────┼─────────────────┼───────────────────┐
│         │                 │                 │                   │
│  ┌──────┴───────┐  ┌──────┴───────┐  ┌──────┴───────┐          │
│  │  LCDEngine   │  │ ServoEngine  │  │  LEDEngine   │          │
│  │              │  │              │  │              │          │
│  │ • listeners  │  │ • listeners  │  │ • listeners  │          │
│  │ • notify()   │  │ • notify()   │  │ • notify()   │          │
│  └──────▲───────┘  └──────▲───────┘  └──────▲───────┘          │
│         │                 │                 │                   │
│         │ onPinChange     │ setAngleFrom    │ onPinChange       │
│         │                 │   Timer1        │                   │
└─────────┼─────────────────┼─────────────────┼───────────────────┘
          │                 │                 │
          │                 │                 │
┌─────────┼─────────────────┼─────────────────┼───────────────────┐
│         │                 │                 │                   │
│  ┌──────┴─────────────────┴─────────────────┴───────┐          │
│  │          Hardware Abstraction Layer (HAL)        │          │
│  │                                                   │          │
│  │  • writePort() → route to LCDEngine              │          │
│  │  • writePort() → route to LEDEngine              │          │
│  │  • (ServoEngine called directly by wrapper)      │          │
│  └──────▲────────────────────────────────────────────┘          │
│         │                                                       │
└─────────┼───────────────────────────────────────────────────────┘
          │
          │ Read registers
          │
┌─────────┼───────────────────────────────────────────────────────┐
│         │                                                       │
│  ┌──────┴────────────────────────────────────────────┐          │
│  │            AVR8jsWrapper                          │          │
│  │                                                   │          │
│  │  step() {                                         │          │
│  │    avrInstruction(cpu);                           │          │
│  │    cpu.tick();                                    │          │
│  │    checkPortChanges();    ← Read PORTB/C/D       │          │
│  │    observeTimer1();       ← Read Timer1          │          │
│  │  }                                                │          │
│  └──────▲────────────────────────────────────────────┘          │
│         │                                                       │
│  ┌──────┴────────────────────────────────────────────┐          │
│  │              AVR CPU (avr8js)                     │          │
│  │                                                   │          │
│  │  • Program Memory (Flash)                        │          │
│  │  • Data Memory (SRAM + Registers)                │          │
│  │  • Timer0 (millis)                               │          │
│  │  • Instruction Decoder                           │          │
│  └───────────────────────────────────────────────────┘          │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🔐 Security Model (Read-Only Observers)

```
┌─────────────────────────────────────────────────────────────────┐
│                        CPU Registers                            │
│                     (Protected State)                           │
│                                                                 │
│  PORTB: 0b00101000  ← Only AVR instructions can write          │
│  PORTC: 0b00000000  ← Only AVR instructions can write          │
│  PORTD: 0b11010101  ← Only AVR instructions can write          │
│  ICR1:  40000       ← Only AVR instructions can write          │
│  OCR1A: 3000        ← Only AVR instructions can write          │
│                                                                 │
│  ┌────────────────────────────────────────────────────┐        │
│  │         Read-Only Access (Observers)               │        │
│  │                                                    │        │
│  │  checkPortChanges() {                             │        │
│  │    const portB = cpu.data[PORTB];  ← ✅ READ      │        │
│  │    // cpu.data[PORTB] = 0;         ← ❌ WRITE     │        │
│  │  }                                                 │        │
│  │                                                    │        │
│  │  observeTimer1() {                                │        │
│  │    const icr1 = cpu.data[ICR1];    ← ✅ READ      │        │
│  │    // cpu.data[ICR1] = 0;          ← ❌ WRITE     │        │
│  │  }                                                 │        │
│  └────────────────────────────────────────────────────┘        │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

**Rule:** Observers can **READ** registers, but **NEVER WRITE**.

---

**Last Updated:** 2025-12-26  
**Architecture:** ✅ **WOKWI-COMPLIANT**
